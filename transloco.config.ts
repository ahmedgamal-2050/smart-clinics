import { TranslocoGlobalConfig } from '@jsverse/transloco-utils';

const config: TranslocoGlobalConfig = {
  rootTranslationsPath: 'apps/dashboard/src/assets/i18n/',
  langs: ['en', 'ar'],
  keysManager: {},
};

export default config;
